<div class="container">
  <h1 class="mt-4 mb-4 text-center">Edit Ingredient</h1>

  <% flash.each do |name, msg| -%>
    <%= content_tag :div, msg, class: "alert alert-#{name}" %>
  <% end -%>

  <%= form_with url: user_recipe_recipe_food_path(@recipe.user, @recipe, @recipe_food), method: :patch, model: @recipe_food, class: 'new-ingredient-form mx-auto' do |form| %>
    <div class="new-ingredient-form-group">
      <%= form.label :quantity %>
      <%= form.number_field :quantity, min: 1, required: true, class: 'new-ingredient-form-control' %>
    </div>

    <div class="new-ingredient-form-group mt-2">
      <%= form.label :food_id %>
      <%= form.collection_select :food_id, current_user.foods, :id, :name, { include_blank: false }, class: 'new-ingredient-form-control' %>
    </div>

    <div class="new-ingredient-form-group mt-2 text-center">
      <%= form.submit "Update", class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>